// write a c program to check the given numbers is +ve value
#include <stdio.h>

int main() {
    int x;
    printf("Enter the x value:");
    scanf("%d",&x);
    int res=(x>0);
    printf("The %d is positive value is %d",x,res);
     return 0;
     }
    